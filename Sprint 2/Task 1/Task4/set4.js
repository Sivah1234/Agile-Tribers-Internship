function compare(a,b)
{
    if(a >b){
        console.log(`${a} is big`);
    }
    else if(a <b)
    {
        console.log(`${b} is small`);
    }
    else {
        console.log("Both number are equal");
    }
}
compare(6,7);